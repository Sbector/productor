---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div
  class={`flex flex-col justify-center
  min-h-screen max-w-full
  py-20 px-10 snap-start
  md:w-[700px] md:mx-auto
  lg:w-[820px]
  xl:w-[1024px]
  ${className}`}
>
  <slot />
</div>
